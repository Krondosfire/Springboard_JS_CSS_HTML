let eventPlace = "BASILICA"; // It depends on the key but is supose to change
const keyCipher = 5;       // value does not change
let classifiedInfo = "";  // It should change after encryption
let unclassifiedInfo = ""; // It will change upon decryption
let crashFree = false;   // If the cipher works, this will remain the same